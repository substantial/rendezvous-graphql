# Substantial Dev Rendezvous - Graphql

This is the example app for the Graphql talk.

### Dependencies:

- Node (nvm, asdf)

### Setup:

- Clone this repo
- Install packages: `yarn|npm install`
- Run app: `yarn|npm start`
